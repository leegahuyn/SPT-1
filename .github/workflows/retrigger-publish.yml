name: Retired standalone publication dispatcher

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Explain retirement
        run: |
          echo "This SPT-1 copy-based publication route is retired."
          echo "Publication and verification now target the complete leegahuyn/mathlib4 frozen-pre-release-2026-08-24 branch."
